\name{buffon}
\alias{buffon}
\title{buffon experiment}
\usage{
buffon(n, L, a)
}
\description{
do the buffon experiment
n: sample numbers；L: the length of needle；a: the distance between lines(L < a)
print the estimate of pi
}
\examples{
buffon(10000, 0.8, 1)
}
